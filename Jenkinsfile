pipeline {
  agent any
  stages {
    stage('run selenide-straigthforward-tests') {
      steps {
        sh '''ls
cd java
ls
cd selenide-junit
ls
cd google-search-straightforward
ls
mvn --version'''
      }
    }
  }
}